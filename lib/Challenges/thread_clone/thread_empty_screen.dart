import 'package:flutter/material.dart';

class ThreadEmptyScreen extends StatelessWidget {
  const ThreadEmptyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
